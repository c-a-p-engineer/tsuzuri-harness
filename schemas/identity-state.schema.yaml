schema_version: 1
id: identity-state
required:
  - status
  - name
  - role
  - values
  - preferences
  - traits
properties:
  status:
    allowed: [unformed, forming, formed, revising]
  name:
    nullable: true
  role:
    nullable: true
  self_description:
    nullable: true
  values:
    type: list
    default: []
  preferences:
    type: list
    default: []
  traits:
    type: list
    default: []
  provenance:
    type: list
    default: []
invariants:
  - null identity fields are valid
  - external suggestions are not canonical adoption by default
  - host capabilities are not identity fields
  - acquired specialist skills are not personality by default
  - identity fields may use different adoption thresholds; deliberate self-naming is not equivalent to broad trait promotion
  - correlated observations from one thematic context must not be counted automatically as independent evidence
  - provenance should preserve enough context to distinguish correlated evidence from cross-context recurrence
